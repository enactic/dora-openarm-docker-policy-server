# Development

## How to release

```bash
git clone git@github.com:enactic/dora-openarm-docker-policy-server.git
cd dora-openarm-docker-policy-server
dev/release.sh ${VERSION} # e.g. dev/release.sh 1.0.0
```
